name: tradebinder
description: "A Magic: the Gathering trade value comparison tool."

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.3
  dio: ^3.0.10
  flutter_image: ^3.0.0
  flutter_local_notifications: ^1.4.4+3
  hive: ^1.4.1+1
  hive_flutter: ^0.3.0+2
  intl: ^0.16.1
  path_provider: ^1.6.5

dev_dependencies:
  hive_generator: ^0.7.0+2
  build_runner: ^1.8.0
  pedantic: ^1.9.0

flutter:
  uses-material-design: true
  assets:
    - assets/splash.png
    - assets/secret.json
